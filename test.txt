// 1. Math and Strings
print "--- Testing Math & Strings ---";
let greeting = "hello, compiler!";
print greeting.toUpperCase();
print "Square root of 81 is: " + Math.sqrt(81.0);
print "Random number: " + Math.random();

// 2. Arrays
print "--- Testing Arrays ---";
let list = [10, 20, 30];
list.push(40);
print "Array length: " + list.len;
print "Second item: " + list[1];

// 3. Classes and Inheritance
print "--- Testing OOP ---";
class Animal {
    constructor(name) {
        this.name = name;
    }

    speak() {
        print this.name + " makes a sound.";
    }
}

class Dog extends Animal {
    constructor(name) {
        super.constructor(name);
    }

    speak() {
        print this.name + " barks loudly!";
    }
}

// Note: No 'new' keyword needed in your language!
let myDog = Dog("Rex");
myDog.speak();

// 4. Reactive UI
print "--- Launching UI ---";

// Create reactive state holders
let counterState = State(0);
let textState = State("Type something...");

// Build the declarative UI tree
Window("My Custom Language UI") {
    Column(spacing = 15.0) {
        Text(content="Welcome to my UI Framework!", fontSize=24.0, color="blue");

        Row(spacing=10.0) {
            Button(text="Decrement") {
                counterState.set(counterState.get() - 1);
            };

            Observe(state=counterState) {
                Text(content="Count: " + counterState.get(), fontSize=18.0, color="black");
            };

            Button(text="Increment") {
                counterState.set(counterState.get() + 1);
            };
        };

        Text(content="Reactivity Test:", fontSize=18.0, color="gray");
        TextField(state=textState);

        Observe(state=textState) {
            Text(content="You typed: " + textState.get(), fontSize=14.0, color="green");
        };
    };
};