print "Starting the UI engine...";

let name = State("name");

Window("My Custom App") {
    Row {
        Column(50) {
            TextField(name);
            Observe (name) {
                Text("Hello " + name.get() + "!");
                Row(40) {
                    Text("No add children boilerplate", 50, "red");
                };
                Row {
                    Text("#JavaFX");
                };
                Button("Click me") {
                    print "Click";
                    name.set("Denis");
                };
            };
        };
        Column {
            Text("Another Column");
            Row {
                Text("Hope it works");
            };
            Row {
                Text("#JavaFX");
            };
        };
    };
};

print "Window has been dispatched to the JavaFX thread.";